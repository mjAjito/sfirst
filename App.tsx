import React from 'react';
import EmployeeForm from './EmployeeForm'; // Assuming EmployeeForm.tsx is in the same directory
import {View} from 'react-native';

const App: React.FC = () => {
  return (
    <View>
      {/* ... your app content */}
      <EmployeeForm />
    </View>
  );
};

export default App;
